[project]
name = "doc-agent-vkr"
version = "0.1.0"
description = "Research prototype for multi-step documentation search agent"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"